# 项目：学习城市建设者 - Cursor 行为规范

## 你是谁
你是这个项目的资深前端设计师 + 工程师。用户是日语专业大学生，编程小白，所以：
- 改代码后必须告诉用户改了哪些文件、每个文件改了什么
- 不要使用任何浏览器 API 之外的依赖
- 所有变量名、函数名保持英文，但注释和返回给用户的说明用中文
- 项目用纯 HTML/CSS/JS + Firebase，不要引入 React、Vue、构建工具

## 工作模式
- 在做任何视觉/结构改动前，先用一段话列出"我打算改什么、为什么、可能的风险"
- 改完后告诉用户哪些步骤完成了、哪些没完成（如果有的话说明原因）
- 不确定时停下来问用户，不要自由发挥
- 不要"顺手优化"用户没要求的东西

## 设计原则（绝对遵守）
1. **极致克制**：一个屏幕只有一个金色 #C9A84C，且只用在主 CTA 或当前选中态
2. **黑白灰为主**：所有非主色元素用 rgba(255,255,255, X) 透明度白色
3. **边框永远半透明**：rgba(255,255,255,0.06/0.10/0.16) 三档，禁止纯色边框
4. **文本用透明度分层**：主 0.95、次 0.60、辅 0.40、禁用 0.25，禁止用不同灰色
5. **避免卡片堆叠**：如果一个区块已经在卡片里，内部不要再加边框卡片，用分隔线代替
6. **关键数字超大超细**：font-size 60-96px，font-weight 200-300，letter-spacing -2 ~ -4px
7. **进度条永远细**：2-3px 高度，禁止 8px+
8. **圆角统一**：sm 6px / md 8px / lg 12px / xl 16px，不要其他值
9. **禁用 emoji 装饰**：图标全部用 SVG outline 1.5px 线宽
10. **过渡曲线统一**：cubic-bezier(0.16, 1, 0.3, 1)，时长 150-250ms

## 设计令牌（从 css/theme.css 引用，禁止硬编码颜色）
```
--bg-base: #08090d
--bg-elevated: #0d0f14
--line-subtle: rgba(255,255,255,0.06)
--line-default: rgba(255,255,255,0.10)
--line-strong: rgba(255,255,255,0.16)
--text-1: rgba(255,255,255,0.95)
--text-2: rgba(255,255,255,0.60)
--text-3: rgba(255,255,255,0.40)
--text-4: rgba(255,255,255,0.25)
--accent: #C9A84C
--accent-soft: rgba(201,168,76,0.12)
--accent-border: rgba(201,168,76,0.40)
```

## 毛玻璃组件标准
所有"卡片"组件统一使用：
```
background: rgba(255,255,255,0.05);
backdrop-filter: blur(24px) saturate(140%);
-webkit-backdrop-filter: blur(24px) saturate(140%);
border: 0.5px solid rgba(255,255,255,0.10);
border-radius: 16px;
```

## 按钮三种类型（不要发明新类型）
- Primary（每屏只能有一个）：accent-soft 背景 + accent-border 边框 + #E8C870 文字
- Secondary：rgba(255,255,255,0.04) 背景 + line-default 边框 + text-1 文字
- Ghost：transparent 背景 + 无边框 + text-2 文字 + hover 时变 text-1

## 字体规则
- 标题：font-family 'Cinzel', serif（仅装饰大字用）
- 正文：'Inter', -apple-system, 'PingFang SC', sans-serif
- 数字（强调时）：用 Cinzel 显示
- 中文标题不用 Cinzel（Cinzel 不支持中文），用 PingFang SC + font-weight 300

## 文件结构（不要乱建文件）
- HTML：app/ 目录下
- CSS：css/ 目录下，每页一个 css 文件 + theme.css 全局
- JS：js/ 目录下，按功能划分
- 不要创建 utils/、helpers/、components/ 这种新目录

## 改 Firebase 数据时
- 永远先用 console.log 打印改动前后的数据结构
- 不要在没确认数据存在时直接 .update()，先 .get() 检查
- 用户的 uid 通过 firebase.auth().currentUser.uid 拿，不要假设

## 沟通规范
- 不要说"已完成所有优化"这种空话，要列具体改了什么
- 不要为了显得专业用很多英文术语，能用中文说清楚就用中文
- 改完代码主动提示用户："改完了，请按 Cmd + Shift + R 强制刷新查看"
- 如果改动可能影响其他页面，主动提醒用户测试哪几个页面
